{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Download Stock",
            "type": "python",
            "request": "launch",
            "module": "ingestion.download_stock",
            "cwd": "${workspaceFolder}"
        }
    ]
}